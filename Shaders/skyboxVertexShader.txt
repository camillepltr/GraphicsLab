/*#version 330 core

layout (location = 0) in vec3 aPos;

out vec3 tex_coords;

uniform mat4 proj;
uniform mat4 view;

void main()
{
    tex_coords = aPos;
    gl_Position = proj * view * vec4(aPos, 1.0);
}  */

#version 400

in vec3 vp;
uniform mat4 proj;
uniform mat4 view;

out vec3 texcoords;

void main() {
  texcoords = vp;
  gl_Position = proj * view * vec4(vp, 1.0);
}